---
title: Post Categories
seo_title: Blog Categories | Jenish Jain's Engineering Blog
description: Browse engineering blog posts by category - productivity, automation, work culture, and more tech topics.
permalink: /categories/
---

{% extends "default.njk" %}

{% block seo %}
<title>{{ seo_title }}</title>
<link rel="canonical" href="{{ siteUrl }}/categories/" />
<meta name="description" content="{{ description }}" />
<meta name="keywords" content="engineering blog, tech categories, productivity, automation, rapido" />

<meta property="og:type" content="website" />
<meta property="og:url" content="{{ siteUrl }}/categories/" />
<meta property="og:title" content="{{ seo_title }}" />
<meta property="og:description" content="{{ description }}" />
<meta property="og:site_name" content="Jenish Jain's Engineering Blog" />

<meta name="twitter:card" content="summary" />
<meta name="twitter:creator" content="@jenishjain6" />
<meta name="twitter:title" content="{{ seo_title }}" />
<meta name="twitter:description" content="{{ description }}" />
{% endblock %}

{% block main %}
<main>
  <div class="heading" style="text-align: center; margin-bottom: 3rem;">
    <h1>{{ title }}</h1>
    <p class="lede">{{ description }}</p>
  </div>

  <div style="max-width: 800px; margin: 0 auto; display: grid; gap: 2rem;">
    {# Productivity Category #}
    {% set productivityPosts = collections.posts | selectattr("data.category", "string") | selectattr("data.category", "search", "productivity") %}
    {% if productivityPosts | length > 0 %}
    <section>
      <h2 style="font-size: 1.5rem; margin-bottom: 1.5rem; padding-bottom: 0.5rem; border-bottom: 2px solid var(--color-yellow-500); color: var(--color-text-heading);">
        <a href="/category/productivity/" style="text-decoration: none; color: inherit;">Productivity</a>
        <span style="font-size: 0.875rem; color: var(--color-text-muted); font-weight: 400; margin-left: 0.5rem;">({{ productivityPosts | length }})</span>
      </h2>
      <ul style="list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 1rem;">
        {% for post in productivityPosts | reverse %}
        <li style="margin: 0;">
          <article style="padding: 1rem; border-left: 3px solid var(--color-border);" onmouseover="this.style.borderColor='var(--color-yellow-500)';" onmouseout="this.style.borderColor='var(--color-border)';">
            <h3 style="font-size: 1rem; margin: 0 0 0.5rem 0; padding: 0;">
              <a href="/{{ post.data.slug }}/" style="text-decoration: none; color: var(--color-text-heading);">{{ post.data.title }}</a>
            </h3>
            <p style="margin: 0 0 0.5rem 0; font-size: 0.875rem; color: var(--color-text-default); line-height: 1.4;">{{ post.data.description }}</p>
            <time style="font-size: 0.75rem; color: var(--color-text-muted);">{{ post.date.toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) }}</time>
          </article>
        </li>
        {% endfor %}
      </ul>
    </section>
    {% endif %}

    {# Work Culture Category #}
    {% set workCulturePosts = collections.posts | selectattr("data.category", "string") | selectattr("data.category", "search", "work-culture") %}
    {% if workCulturePosts | length > 0 %}
    <section>
      <h2 style="font-size: 1.5rem; margin-bottom: 1.5rem; padding-bottom: 0.5rem; border-bottom: 2px solid var(--color-yellow-500); color: var(--color-text-heading);">
        <a href="/category/work-culture/" style="text-decoration: none; color: inherit;">Work Culture</a>
        <span style="font-size: 0.875rem; color: var(--color-text-muted); font-weight: 400; margin-left: 0.5rem;">({{ workCulturePosts | length }})</span>
      </h2>
      <ul style="list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 1rem;">
        {% for post in workCulturePosts | reverse %}
        <li style="margin: 0;">
          <article style="padding: 1rem; border-left: 3px solid var(--color-border);" onmouseover="this.style.borderColor='var(--color-yellow-500)';" onmouseout="this.style.borderColor='var(--color-border)';">
            <h3 style="font-size: 1rem; margin: 0 0 0.5rem 0; padding: 0;">
              <a href="/{{ post.data.slug }}/" style="text-decoration: none; color: var(--color-text-heading);">{{ post.data.title }}</a>
            </h3>
            <p style="margin: 0 0 0.5rem 0; font-size: 0.875rem; color: var(--color-text-default); line-height: 1.4;">{{ post.data.description }}</p>
            <time style="font-size: 0.75rem; color: var(--color-text-muted);">{{ post.date.toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) }}</time>
          </article>
        </li>
        {% endfor %}
      </ul>
    </section>
    {% endif %}

    {# Self Growth #}
    {% set selfGrowthPosts = collections.posts | selectattr("data.category", "string") | selectattr("data.category", "search", "self-growth") %}
    {% if selfGrowthPosts | length > 0 %}
    <section>
      <h2 style="font-size: 1.5rem; margin-bottom: 1.5rem; padding-bottom: 0.5rem; border-bottom: 2px solid var(--color-yellow-500); color: var(--color-text-heading);">
        <a href="/category/self-growth/" style="text-decoration: none; color: inherit;">Self Growth</a>
        <span style="font-size: 0.875rem; color: var(--color-text-muted); font-weight: 400; margin-left: 0.5rem;">({{ selfGrowthPosts | length }})</span>
      </h2>
      <ul style="list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 1rem;">
        {% for post in selfGrowthPosts | reverse %}
        <li style="margin: 0;">
          <article style="padding: 1rem; border-left: 3px solid var(--color-border);" onmouseover="this.style.borderColor='var(--color-yellow-500)';" onmouseout="this.style.borderColor='var(--color-border)';">
            <h3 style="font-size: 1rem; margin: 0 0 0.5rem 0; padding: 0;">
              <a href="/{{ post.data.slug }}/" style="text-decoration: none; color: var(--color-text-heading);">{{ post.data.title }}</a>
            </h3>
            <p style="margin: 0 0 0.5rem 0; font-size: 0.875rem; color: var(--color-text-default); line-height: 1.4;">{{ post.data.description }}</p>
            <time style="font-size: 0.75rem; color: var(--color-text-muted);">{{ post.date.toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) }}</time>
          </article>
        </li>
        {% endfor %}
      </ul>
    </section>
    {% endif %}

    {# Automation #}
    {% set automationPosts = collections.posts | selectattr("data.category", "string") | selectattr("data.category", "search", "automation") %}
    {% if automationPosts | length > 0 %}
    <section>
      <h2 style="font-size: 1.5rem; margin-bottom: 1.5rem; padding-bottom: 0.5rem; border-bottom: 2px solid var(--color-yellow-500); color: var(--color-text-heading);">
        <a href="/category/automation/" style="text-decoration: none; color: inherit;">Automation</a>
        <span style="font-size: 0.875rem; color: var(--color-text-muted); font-weight: 400; margin-left: 0.5rem;">({{ automationPosts | length }})</span>
      </h2>
      <ul style="list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 1rem;">
        {% for post in automationPosts | reverse %}
        <li style="margin: 0;">
          <article style="padding: 1rem; border-left: 3px solid var(--color-border);" onmouseover="this.style.borderColor='var(--color-yellow-500)';" onmouseout="this.style.borderColor='var(--color-border)';">
            <h3 style="font-size: 1rem; margin: 0 0 0.5rem 0; padding: 0;">
              <a href="/{{ post.data.slug }}/" style="text-decoration: none; color: var(--color-text-heading);">{{ post.data.title }}</a>
            </h3>
            <p style="margin: 0 0 0.5rem 0; font-size: 0.875rem; color: var(--color-text-default); line-height: 1.4;">{{ post.data.description }}</p>
            <time style="font-size: 0.75rem; color: var(--color-text-muted);">{{ post.date.toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) }}</time>
          </article>
        </li>
        {% endfor %}
      </ul>
    </section>
    {% endif %}

    {# DIY #}
    {% set diyPosts = collections.posts | selectattr("data.category", "string") | selectattr("data.category", "search", "diy") %}
    {% if diyPosts | length > 0 %}
    <section>
      <h2 style="font-size: 1.5rem; margin-bottom: 1.5rem; padding-bottom: 0.5rem; border-bottom: 2px solid var(--color-yellow-500); color: var(--color-text-heading);">
        <a href="/category/diy/" style="text-decoration: none; color: inherit;">DIY</a>
        <span style="font-size: 0.875rem; color: var(--color-text-muted); font-weight: 400; margin-left: 0.5rem;">({{ diyPosts | length }})</span>
      </h2>
      <ul style="list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 1rem;">
        {% for post in diyPosts | reverse %}
        <li style="margin: 0;">
          <article style="padding: 1rem; border-left: 3px solid var(--color-border);" onmouseover="this.style.borderColor='var(--color-yellow-500)';" onmouseout="this.style.borderColor='var(--color-border)';">
            <h3 style="font-size: 1rem; margin: 0 0 0.5rem 0; padding: 0;">
              <a href="/{{ post.data.slug }}/" style="text-decoration: none; color: var(--color-text-heading);">{{ post.data.title }}</a>
            </h3>
            <p style="margin: 0 0 0.5rem 0; font-size: 0.875rem; color: var(--color-text-default); line-height: 1.4;">{{ post.data.description }}</p>
            <time style="font-size: 0.75rem; color: var(--color-text-muted);">{{ post.date.toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) }}</time>
          </article>
        </li>
        {% endfor %}
      </ul>
    </section>
    {% endif %}
  </div>
</main>
{% endblock %}
